---
import Icons from "../components/Icons.astro";
import Icon from "../components/Icon.astro";

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;700&family=Roboto+Flex:opsz@8..144&display=swap"
      rel="stylesheet"
    />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <slot name="links" />
  </head>
  <body class="center-content">
    <Icons class="sr-only" />

    <header class="full-bleed">
      <!-- <h1>Ariana for Benecia School Board</h1> -->
      <nav class="primary">
        <ul class="links" aria-label="primary navigation links">
          <li>
            <a href="/" class="home">
              <img src="logo.svg" />
            </a>
          </li>
          <li><a href="/about">About</a></li>
          <li><a href="/get-involved">Get Involved</a></li>
          <li>
            <!-- <a href="/donate">Donate</a> -->
            <a
              href="https://secure.fundhero.com/ariana-martinez-for-benicia-school-board-2023"
              >Donate</a
            >
          </li>
          <!-- <li>
            <a
              class="donate-link"
              href="https://politics.raisethemoney.com/en/ariana-martinez-for-benicia-school-board-2023"
              >Donate</a>
          </li> -->
        </ul>
      </nav>
    </header>

    <slot />

    <footer class="full-bleed">
      <nav class="socials">
        <ul aria-label="social media links">
          <li>
            <a hidden>
              <Icon href="facebook" class="socials-icon" />
              <span class="sr-only">Facebook</span>
            </a>
          </li>
          <li>
            <a href="https://instagram.com/ariana4busd">
              <Icon href="instagram" class="socials-icon" />
              <span class="sr-only">Instagram</span>
            </a>
          </li>
          <li>
            <a href="https://twitter.com/ariana4busd">
              <Icon href="twitter" class="socials-icon" />
              <span class="sr-only">Twitter</span>
            </a>
          </li>
        </ul>
      </nav>

      <a href="/" class="logo">
        <img src="logo.svg" />
      </a>

      <!-- https://www.fppc.ca.gov/learn/campaign-rules/campaign-advertising-requirements-restrictions.html -->
      <p class="campaign-ad-disclosure">
        Paid for by Ariana Martinez for Benicia School Board 2023 – FPPC ID
        #1457993
      </p>
    </footer>

    <style is:global>
      :root {
        --🎨page: white;
        --🎨page_text1: hsl(0deg 0% 0%);
        --🎨page_text2: hsl(0deg 0% 30%);

        --🎨lteal: hsl(176deg 67% 39%);
        --🎨lteal: hsl(176deg 67% 79%);
        --🎨lteal_text1: hsl(176deg 67% 4%);

        --🎨teal: hsl(175deg 24% 19%);
        --🎨teal_pop1: hsl(173deg 100% 80%);
        --🎨teal_text1: hsl(176deg 74% 92%);

        --max-content-width: 1024px;
        --min-touch-size: 48px;
      }

      html {
        font-family: system-ui, sans-serif;
        background-color: #f6f6f6;
      }

      code {
        font-family: Menlo, Monaco, Lucida Console, Liberation Mono,
          DejaVu Sans Mono, Bitstream Vera Sans Mono, Courier New, monospace;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        /* font-family: "Roboto Flex", sans-serif; */
        /* overlay over scroll bounce background */
        background: white;

        display: grid;
        gap: 0 1rem;
        grid-auto-rows: min-content;
        grid-template-columns:
          1fr
          [center] minmax(320px, var(--max-content-width))
          1fr;
      }

      body > * {
        grid-column: center;
      }

      .full-bleed {
        grid-column: 1/-1;
        display: grid;
        gap: 0 1rem;
        grid-auto-rows: min-content;
        grid-template-columns:
          1fr
          [center] minmax(320px, var(--max-content-width))
          1fr;
      }

      .full-bleed > * {
        grid-column: center;
      }

      .sr-only {
        position: absolute;
        width: 1px;
        height: 1px;
        padding: 0;
        margin: -1;
        clip: rect(0 0 0 0);
        clip-path: inset(50%);
        overflow: hidden;
        white-space: nowrap;
      }
    </style>

    <style>
      nav :is(ul, li) {
        display: contents;
      }

      nav a {
        min-width: var(--min-touch-size);
        min-height: var(--min-touch-size);
        text-decoration: none;
      }

      footer {
        justify-items: center;
        min-height: 25vh;
        padding-block: 1rem;
        background: var(--🎨teal);
        color: var(--🎨teal_text1);
      }

      /* scroll bounce background */
      footer::after {
        content: "";
        position: fixed;
        inset: auto 0 0 0;
        z-index: -1;
        height: 50vh;
        background: var(--🎨teal);
      }

      footer .logo {
        width: 30%;
      }

      footer .logo img {
        width: 100%;
      }

      header {
        background: var(--🎨teal);
        color: var(--🎨teal_pop1);
      }

      nav.primary {
        display: flex;
        gap: 1rem;
        align-items: end;
      }

      nav.primary a {
        display: grid;
        padding: 0.5rem;
        place-items: center;
        font-weight: 700;
      }

      nav.primary a:visited {
        color: var(--🎨teal);
        color: currentColor;
      }

      nav.primary a.home {
        width: 8rem;
        border: 2px solid var(--🎨teal_pop1);
        padding: 0;
        margin: 0.5rem 0;
        margin-right: auto;
      }

      nav.primary a.home img {
        width: 100%;
      }

      nav.socials {
        display: flex;
        gap: 0.5rem;
        justify-self: end;
      }

      nav.socials a {
        width: var(--min-touch-size);
        height: var(--min-touch-size);
        padding: 0.25rem;
        color: var(--🎨teal_text1);
      }

      .socials-icon {
        width: 100%;
        height: 100%;
      }
    </style>
  </body>
</html>
